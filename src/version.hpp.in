/// Version information for TypeTrace (generated by CMake)

#ifndef TYPETRACE_VERSION_HPP
#define TYPETRACE_VERSION_HPP

#include <string_view>

namespace typetrace {

// ============================================================================
// Build Information
// ============================================================================

/// Application version string
constexpr std::string_view PROJECT_VERSION = "@PROJECT_VERSION@";

/// Application name
constexpr std::string_view PROJECT_NAME = "@PROJECT_NAME@";

/// Git commit hash (if available)
constexpr std::string_view PROJECT_GIT_COMMIT = "@GIT_COMMIT@";

/// Build type (Debug/Release)
constexpr std::string_view PROJECT_BUILD_TYPE = "@CMAKE_BUILD_TYPE@";

} // namespace typetrace

#endif
