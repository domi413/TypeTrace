using Gtk 4.0;

template $Statistics : Gtk.Box {
    orientation: vertical;
    halign: baseline;

    Gtk.Label {
        label: "Statistics test text";
        margin-start: 5;
    }

    Gtk.DrawingArea drawing_area {
        vexpand: true;
        hexpand: true;
    }
}
